# Store-Conformance Fixtures

Committed, generated artifacts: three tiny Zarr stores (v2 with zlib, v3 with gzip, sharded v3) plus `expected/*.json` read-back oracles. Produced by zarr-python (the reference implementation) so the JS reader is tested against independently produced bytes, and round-trip verified at generation time.

Regenerate from the repo root (do not hand-edit anything under `stores/`):

```
uv run --with zarr --with numpy --with numcodecs tools/spectral-prism-py/scripts/generate_store_fixtures.py
```

Last generated with zarr-python 3.2.1, 2026-07-02. Regeneration is a reviewed change: commit the script change and the regenerated bytes together, and say why.
